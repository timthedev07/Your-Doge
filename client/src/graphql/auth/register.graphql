mutation Register(
  $email: String!
  $password: String!
  $username: String!
  $recaptchaToken: String!
) {
  register(
    email: $email
    password: $password
    username: $username
    recaptchaToken: $recaptchaToken
  )
}
